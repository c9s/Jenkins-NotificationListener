This is Perl module Jenkins::NotificationListener.

INSTALLATION

Jenkins::NotificationListener installation is straightforward. If your CPAN shell is set up,
you should just be able to do

    % cpan Jenkins::NotificationListener

Download it, unpack it, then build it as per the usual:

    % perl Makefile.PL
    % make && make test

Then install it:

    % make install

DOCUMENTATION

Jenkins::NotificationListener documentation is available as in POD. So you can do:

    % perldoc Jenkins::NotificationListener

to read the documentation online with your favorite pager.

Yo-An Lin
